@model IEnumerable<GymTest.Models.MedicalEmergency>

@{
    ViewData["Title"] = "Index";
}

<h2>Emergencias médica</h2>

<p>
    <a asp-action="Create" class="btn btn-primary">Crear nueva</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.MedicalEmergencyDescription)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.MedicalEmergencyDescription)
                </td>
                <td>
                    <a asp-action="Edit" asp-route-id="@item.MedicalEmergencyId" class="btn btn-primary">Modificar</a>
                    <a asp-action="Details" asp-route-id="@item.MedicalEmergencyId" class="btn btn-primary">Detalle</a>
                    <a asp-action="Delete" asp-route-id="@item.MedicalEmergencyId" class="btn btn-danger">Eliminar</a>
                </td>
            </tr>
        }
    </tbody>
</table>
