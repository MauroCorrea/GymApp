@model IEnumerable<GymTest.Models.Role>

@{
    ViewData["Title"] = "Index";
}

<h2>Administración de Roles</h2>

<p>
    <a asp-action="Create" class="btn btn-primary">Nuevo rol</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.RoleDescription)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.RoleDescription)
                </td>
                <td>
                    <div class="btn-group">
                        <a asp-action="Edit" asp-route-id="@item.RoleId" class="btn btn-primary">Modificar</a>
                        <a asp-action="Delete" asp-route-id="@item.RoleId" class="btn btn-danger">Eliminar</a>
                    </div>
                </td>
            </tr>
        }
    </tbody>
</table>
