@model GymTest.Models.CashSubcategory

@{
    ViewData["Title"] = "Create";
}

<h2>Crear</h2>

<h4>Subcategoría</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Create">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <div class="form-group">
                <label asp-for="CashSubcategoryDescription" class="control-label"></label>
                <input asp-for="CashSubcategoryDescription" class="form-control" />
                <span asp-validation-for="CashSubcategoryDescription" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="CashCategoryId" class="control-label"></label>
                <select asp-for="CashCategoryId" class="form-control" asp-items="ViewBag.CashCategoryId"></select>
            </div>
            <div class="form-group">
                <input type="submit" value="Create" class="btn btn-success" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index" class="btn btn-default">Regresar</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
