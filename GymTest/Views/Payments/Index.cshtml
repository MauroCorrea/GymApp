@model IEnumerable<GymTest.Models.Payment>

@{
    ViewData["Title"] = "Administración de Pagos";
}

<br />
<h2>Administración de Pagos</h2>

<p>
    <a asp-action="Create" class="btn btn-primary">Agregar Pago</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.PaymentDate)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Amount)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.User)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.PaymentDate)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Amount)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.User.DocumentNumber)
            </td>
            <td>
                <div class="btn-group">
                    <a asp-action="Edit" asp-route-id="@item.PaymentId" class="btn btn-primary">Edit</a>
                    <a asp-action="Details" asp-route-id="@item.PaymentId" class="btn btn-primary">Details</a>
                    <a asp-action="Delete" asp-route-id="@item.PaymentId" class="btn btn-danger">Delete</a>
                </div>
            </td>
        </tr>
}
    </tbody>
</table>
