@model GymTest.Models.CashCategory

@{
    ViewData["Title"] = "Edit";
}

<h2>Modificar</h2>

<h4>Categoría de movimiento</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Edit">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <input type="hidden" asp-for="CashCategoryId" />
            <div class="form-group">
                <label asp-for="CashCategoryDescription" class="control-label"></label>
                <input asp-for="CashCategoryDescription" class="form-control" />
                <span asp-validation-for="CashCategoryDescription" class="text-danger"></span>
            </div>
            <div class="form-group">
                <input type="submit" value="Guardar" class="btn btn-success" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index" class="btn btn-default">Regresar</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
