@model IEnumerable<GymTest.Models.Assistance>

@{
    ViewData["Title"] = "Index";
}

<h2>Asistencias</h2>

<p>
    <a asp-action="Create" class="btn btn-primary">Ingrear Asistencia</a>
</p>

<form asp-controller="Assistances" asp-action="Index">
    <p>
        Filtro inteligente: <input type="text" name="SearchString">
        <input type="submit" value="Filtrar" class="btn btn-default" />
    </p>
</form>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.AssistanceDate)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.User)
            </th>
            @*<th></th>*@
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.AssistanceDate)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.User.FullName)
                </td>
                @*<td>
                        <a asp-action="Edit" asp-route-id="@item.AssistanceId">Edit</a> |
                        <a asp-action="Details" asp-route-id="@item.AssistanceId">Details</a> |
                        <a asp-action="Delete" asp-route-id="@item.AssistanceId">Delete</a>
                    </td>*@
            </tr>
        }
    </tbody>
</table>
